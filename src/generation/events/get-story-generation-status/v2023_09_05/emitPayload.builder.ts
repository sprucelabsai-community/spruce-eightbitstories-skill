import { buildSchema } from '@sprucelabs/schema'

const getStoryGenerationStatusEmitPayloadBuilder = buildSchema({
    id: 'getStoryGenerationStatusEmitPayload',
    fields: {},
})

export default getStoryGenerationStatusEmitPayloadBuilder
